import themes from '../../styles/themes';

const progressBarBuild = {
  backgroundColor: themes.lightBlue,
  pathColor: themes.white,
  trailColor: 'transparent',
};

export default progressBarBuild;
